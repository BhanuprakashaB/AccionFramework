package lsac.pages;

import uiauto.central.ContextConfiguration;
import uiauto.client.Client;
import uiauto.page.BasePage;

public class LSATPrepTest71HomePage extends BasePage{

	public LSATPrepTest71HomePage(ContextConfiguration conf, Client client) throws Exception {
		super("LSATPrepTest71Home.json", conf, client);
	}

}
